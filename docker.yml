# Build a service with Dockerfile
version: '1.0'

steps:
  build-example:
    type: build
    description: yaml examplesand purpose taken place jenkins
    image-name: codefresh-io/image
    dockerfile: Dockerfile
    tag: latest_docker
